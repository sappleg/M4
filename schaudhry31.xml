<project name = "Person1AntBuild" basedir="." default = "all">
    <!-- Setting up all the properties -->
    
    <target name="all" depends="run" />
    
   	<target name = "init">
	    <property name = "src.dir" value = "${basedir}/src"/>
	    <property name = "lib.dir" value = "${basedir}/lib"/>
	    <property name = "build.dir" value = "${basedir}/build"/>
	    <property name = "main-class" value = "src.edu.gatech.oad.antlab.person.Person1"/>
    </target>
    
	<target name = "prepare" depends = "init">
	   <mkdir dir="${src.dir}"/>
	   <mkdir dir="${lib.dir}"/>
	   <mkdir dir="${build.dir}"/>
	</target>
	
	<target name = "compile" depends = "prepare">
	    <javac srcdir="${src.dir}" destdir="${build.dir}">
	    </javac>
	</target>
   
	<target name = "run" depends="compile">
	    <java classname="src.edu.gatech.oad.antlab.person.Person1">
	    </java>
	</target>
	
	<target name = "clean" depends = "init" >
	    <delete dir="${build.dir}" />
	</target>
	
</project>